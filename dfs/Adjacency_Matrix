 #include <bits/stdc++.h>
using namespace std;
#define fastcode   ios::sync_with_stdio(false);cin.tie(nullptr);
#define all(x) x.begin(),x.end()
using ll = long long;

void Adjacency_Matrix_non_direct() {
 int n,m;
 cin >> n >> m;
 vector<vector<int>> vec(n+1, vector<int>(n+1,0));
 for (int i = 0; i < m; i++) {
  int u,v;
  cin >> u >> v;
  vec[u][v] = 1;
  vec[v][u] = 1;
 }
 for (int i = 1; i <= n; i++) {
  for (int j = 1; j <= n; j++) {
   cout << vec[i][j] << " ";
  }
  cout << "\n";
 }
}
void Adjacency_Matrix_direct() {
 int n,m;
 cin >> n >> m;
 vector<vector<int>> vec(n+1, vector<int>(n+1,0));
 for (int i = 0; i < m; i++) {
  int u,v;
  cin >> u >> v;
  vec[u][v] = 1;
 }
 for (int i = 1; i <= n; i++) {
  for (int j = 1; j <= n; j++) {
   cout << vec[i][j] << " ";
  }
  cout << "\n";
 }
}


signed main() {




















}
