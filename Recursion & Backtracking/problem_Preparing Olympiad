        #include <bits/stdc++.h>
    using namespace std;
    #define fastcode   ios::sync_with_stdio(false);cin.tie(nullptr);
    #define all(x) x.begin(),x.end()
    #define  Afifi
    #define setCnt              __builtin_popcount
    #define setCntL         __builtin_popcountll
    #define  Come_back
    #define integer int
    // #define  int long long
    using ll = long long;
    int n,l,r,x;
    vector<int>v;
    int magic(int i,int minn=2e9,int maxx=-1,int cnt=0,ll sum=0) {

        if (i==n) {
            return ((maxx-minn)>=x&&sum>=l&&sum<=r&&cnt>=2);
        }
        return magic(i+1,minn,maxx,cnt,sum)+magic(i+1,min(minn,v[i]),max(maxx,v[i]),cnt+1,sum+v[i]);
    }
    int main() {
        cin>>n>>l>>r>>x;
        v.resize(n);
        for (auto &it:v)cin>>it;
        cout<<magic(0)<<endl;










    }
